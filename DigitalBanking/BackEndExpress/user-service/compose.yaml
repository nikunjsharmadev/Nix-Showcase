services:
  api:
    build: .
    ports:
      - '5000:5000'
    env_file:
      - .env.docker
    depends_on:
      - mongo

  mongo:
    image: mongo:8
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
  redis-data:
