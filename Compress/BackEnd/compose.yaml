services:
  api:
    build: .
    ports:
      - '4000:4000'
    env_file:
      - .env.docker
    volumes:
      - uploads:/app/uploads/compressed

  worker:
    build: .
    env_file:
      - .env.docker
    command: npm run worker
    depends_on:
      - redis
    volumes:
      - uploads:/app/uploads/compressed
  redis:
    image: redis:7-alpine
    container_name: redis

    ports:
      - '6379:6379'
    restart: unless-stopped

    volumes:
      - redis-data:/data

volumes:
  uploads:
  redis-data:
